{
  "name": "vivim-live",
  "version": "1.0.0",
  "description": "VIVIM Live - Sovereign AI Interface Landing Page",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun run --watch server.js",
    "build": "echo 'Static build - no build step needed'",
    "start": "bun server.js",
    "preview": "bun server.js",
    "lint": "prettier --write .",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "devDependencies": {
    "prettier": "^3.2.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/owenservera/vivim-live.git"
  }
}
